project TP7 is

   for Main use ("exemple_vecteurs_creux.adb", "test_vecteurs_creux.adb", "vecteurs_creux.adb", "vecteurs_creux.ads");

   package Builder is
      for Default_Switches ("ada") use ("-s");
   end Builder;

   package Compiler is
      for Default_Switches ("ada") use ("-gnatwa", "-gnata", "-g");
   end Compiler;

   package Binder is
      for Default_Switches ("ada") use ("-E");
   end Binder;

end TP7;

